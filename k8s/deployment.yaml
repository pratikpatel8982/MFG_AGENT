apiVersion: apps/v1
kind: Deployment
metadata:
  name: mfg-agent-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mfg-agent
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: mfg-agent
    spec:
      containers:
        - name: mfg-agent
          image: pratikpatel8982/mfg-agent:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: mfg-agent-config
            - secretRef:
                name: mfg-agent-secrets
